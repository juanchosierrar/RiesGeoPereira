{
  "name": "riesgeopereira-root",
  "version": "1.0.0",
  "description": "RiesGeoPereira - Sistema de Gestión de Riesgos Geoespaciales para Pereira",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "postinstall": "npm install --prefix web --legacy-peer-deps",
    "build": "npm run build --prefix web",
    "start": "node web/.next/standalone/server.js",
    "dev": "npm run dev --prefix web"
  }
}
